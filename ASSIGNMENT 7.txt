--Michael Calhoun
--1.
SELECT PART_NUM, DESCRIPITION, PRICE 
FROM PART;

--2.
SELECT * FROM ORDER;

--3.
SELECT CUSTOMER_NAME
FROM CUSTOMER
WHERE CREDIT_LIMIT >= 10000.00;

--4.
SELECT ORDER_NUM, CUSTOMER_NUMBER
FROM ORDER
WHERE ORDER_DATE = '2010-10-23', CUSTOMER NUMBER = '608';

--5. 
SELECT CUSTOMER_NAME, CUSTOMER_NUM
FROM CUSTOMER
WHERE REP_NUM = '35' OR REP_NUM = '65';

--6. 
SELECT PART_NUM, DESCRIPTION
FROM PART
WHERE <> 'AP';

--7.
SELECT PART_NUM, DESCRIPTION, ON_HAND
FROM PART
WHERE ON_HAND BETWEEN 10 AND 25;

--8.
SELECT PART_NUM, DESCRIPTION, ON_HAND_VALUE(ON_HAND*PRICE) --ON_HAND * PRICE AS ON_HAND_VALUE 
FROM PART
WHERE CLASS = 'SG';

--9.
SELECT PART_NUM, DESCRIPTION, ON_HAND_VALUE(ON_HAND*PRICE) --ON_HAND * PRICE AS ON_HAND_VALUE 
FROM PART
WHERE ON_HAND_VALUE >= 7500.00;

--10.
SELECT PART_NUM, DESCRIPTION, 
FROM PART
WHERE CLASS IN ('AP', 'SG');

--11.
SELECT CUSTOMER_NUM, CUSTOMER_NAME
FROM CUSTOMER
WHERE CUSTOMER_NAME LIKE 'B%';

--12.
SELECT * FROM PARTS
ORDER BY DESCRIPTION;

--13.
SELECT * FROM PARTS 
ORDER BY WAREHOUSE, PART_NUM;

--14.
SELECT CUSTOMER_NAME
FROM CUSTOMER
WHERE BALANCE > CREDIT_LIMIT;

--Correct
SELECT COUNT(*) 
FROM CUSTOMER 
WHERE BALANCE > CREDIT_LIMIT;

--15.
SELECT SUM(BALANCE)
FROM CUSTOMER
WHERE BALANCE < CREDIT_LIMIT AND REP_NUM = '65';

--16. 
SELECT PART_NUM, DESCRIPTION, ON_HAND 
FROM PART 
WHERE ON_HAND > (SELECT AVG(ON_HAND) FROM PART);

--18.
SELECT PART_NUM, DESCRIPTION, PRICE 
FROM PART 
WHERE PRICE =  (SELECT IN(PRICE) FROM PART);

--19.
SELECT REP_NUM, SUM(BALANCE)
FROM CUSTOMER
GROUP BY REP_NUM
ORDER BY REP_NUM;

--20.
SELECT REP_NUM, SUM(BALANCE)
FROM CUSTOMER
GROUP BY REP_NUM
HAVING SUM(BALANCE) > 10000.00
ORDER BY REP_NUM;

--21.
SELECT PART_NUM
FROM PART
WHERE DESRIPTION IS NULL;